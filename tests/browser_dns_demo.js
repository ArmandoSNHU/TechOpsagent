() => { document.querySelector('[data-category="network"]').click(); [...document.querySelectorAll('.tool-card')].find(x=>x.querySelector('h2').textContent==='Test DNS resolution').querySelector('button').click(); for(const example of ['dns','dns_failure']) {document.querySelector('#dns-example').value=example;document.querySelector('#run-tool').click();} document.querySelector('[data-category="reports"]').click(); [...document.querySelectorAll('.history-item')].find(x=>x.textContent.includes('portal.example.com')).click(); if(document.querySelector('#dns-example').value!=='dns')throw Error('History mismatch'); return {history:'pass',overflow:document.documentElement.scrollWidth>innerWidth}; }
